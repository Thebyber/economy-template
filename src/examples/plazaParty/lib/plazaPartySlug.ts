/**
 * Economy / portal slug for this sample — used as Colyseus `sceneId` so players of this
 * minigame are grouped together (separate from in-game `plaza` etc.).
 */
export const PLAZA_PARTY_MINIGAME_SLUG = "plaza-party";
