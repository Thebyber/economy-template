/**
 * Fixed production plaza shard — template minigames join here so all guests share one Colyseus room.
 * (Room handler is still `PlazaRoom`; see API `server.config.ts`.)
 */
export const MMO_PRODUCTION_PLAZA_ROOM = "sunflorea_kale" as const;
